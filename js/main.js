/* learn more about the require.js config settings */

/*
requirejs.config({
    baseUrl: 'lib',
    paths: {
        app: '../app'
    }
});
*/

requirejs(['underscore-min']);
requirejs(['jquery-1.11.2.min']);
requirejs(['json2']);
requirejs(['backbone-min']);
requirejs(['utility']);
requirejs(['script']);